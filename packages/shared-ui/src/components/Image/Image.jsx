// import logo from "../../assets/images/logo-mob.png";
// import logo2x from "../../assets/images/logo-mob@2x.png";

// const imageMap = {
//   logo,
//   logo2x,
// };

// const Image = ({ name, alt = "", fallback = null, ...props }) => {
//   const src = imageMap[name];
//   const src2x = imageMap[`${name}2x`];

//   if (!src) {
//     console.warn(`Image "${name}" not found`);
//     return fallback;
//   }

//   return (
//     <img src={src} srcSet={`${src} 1x, ${src2x} 2x`} alt={alt} {...props} />
//   );
// };

// export default Image;
